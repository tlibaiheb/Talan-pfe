apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-claim1
  labels:
    app: postgresql
    tier: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi